package edu.epam.task6.report;

public class ConversionResult {
    public void printConversionResult(int seconds, int hours, int minutes,int remainingSeconds)
    {
        System.out.println("The elapsed time in seconds equal to " + seconds +" seconds is equal to " + hours + " hours " + minutes + " minutes " + remainingSeconds + " seconds ");
    }
}
